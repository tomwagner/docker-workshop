{
  "name": "nodejsapp",
  "version": "1.0.0",
  "description": "example app",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "echo 'NodeJS version: ' && node -v && echo 'NPM version: ' && npm -v && echo 'Yarn version: ' && yarn --version && node index.js"
  },
  "author": "Tom Wagner",
  "dependencies": {
    "express": "^4.15.3"
  }
}
